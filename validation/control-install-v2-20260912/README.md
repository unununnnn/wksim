# Current Control v2 installation check

Fresh candidate `/root/wksim-joint-control-BeqOco`, manifest SHA256 `0b6d6de16bba35acd964828c3d4f220abe1a36b1d5d5533686e76944f1ee3f07`, built with the current planner sender/receiver/release support. The live sealer compared repository, staged and installed sources successfully: 15 Simulator Python files and two frozen message assets, plus the existing Control/message/build checks. No executable or shared library is published here.

The first candidate (`avGyUP`) built, but its installed smoke never received ROS time. A minimal ordinary rclpy Node in the same environment did receive it. Inspection found `PlannerTransportNode` overwrote rclpy's internal `_subscriptions` list, removing the Clock subscription from executor traversal even though DDS discovery still reported the subscription. The node now stores its own references in `_planner_subscriptions`. The original failed manifest/log/error and node source remain under `failed-clock/`.

After the fix, 26 existing and new generated-message/ROS/TCP checks passed, including a non-injected production Clock subscription test. The final installed-only smoke ran from `BeqOco/smoke-outside` with only installed overlays sourced, under private network/IPC/mount namespaces and fresh `/dev/shm`. Actual `/clock` publication initialized the node to 1s. Synthetic owned SessionState and two actual TCP control frames led to CANCELLED and generated SetupRequest request_id=1, mode=BRAKE. The setup was captured rather than published; no command was emitted and release_confirmed stayed false. Every imported Simulator module resolved under the installed candidate prefix, and the node/socket/context were closed.

This proves installation, real ROS-clock servicing, receive-side decoding and request assembly. It does not prove public RMW delivery, native FC mode change, physics, cancellation or Full acceptance. The new candidate has not been promoted into the final-flight admission pins.
