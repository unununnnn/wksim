本轮深度生命周期运行前条件：沿用160x120、90°HFOV、500权威步采样、100m、米制平面Z/NaN/ENU；全部物理1ms/4ms和0.5x/100ms监督保持。地面同一真实双FC联合场景，停掉自建整个UE生产者，确认无新文件且3墙钟秒推进至少1000tick；重开UE得到新stream，至少5个当前采样，并拒绝保留的旧stream通知。正式cold-reset创建新epoch/generation+1，至少5个新epoch采样，拒绝旧epoch通知。重新读取原始物理/clock/浮点深度和点云验证。此场不执行飞行，旧run5实飞证据独立保留。Full传感器故障矩阵不混作本票退出条件。

下一次运行前修订验证方式：保持同一UE进程，使用与现有RGB同型的显式depth_stream_control停用/启用采集；原3秒/1000tick、新stream、旧通知拒绝和真实cold-reset门槛均不变。异步取消可删除尚未交付的旧文件，但不得发布新元数据/通知。整UE重开失败保留，不将此采集重启冒称UE进程重连已通过。
