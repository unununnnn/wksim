# Exclusive-write read-only evidence deliverable (n1+n2 of ds-dll-abi-evidence-20260913-01).
# Disable all EOL conversion inside this directory so the recorded SHA-256 of every
# artifact cannot drift through a CRLF checkout or a core.autocrlf setting.
* -text
