# 已批准的环境与视觉反馈合同

2026-09-07，用户对当前任务明确提出的问题回答：**“采用此环境与视觉合同”**。批准依据为 [剩余门槛提案](plan/remaining-gates-proposal.md) 的环境/视觉方案；原问题、答复和提案 SHA256 保存在 `validation/remaining-gates-20260907/environment-acceptance.json`。

- 首例由 WSL 负责静态地形与碰撞，在权威物理步查询几何；UE 从同一带版本/哈希的场景配置生成显示。单位为米，公共 ENU 和原点明确，UE 换算限于显示边界。
- 静态场景加载失败或哈希不符时启动前拒绝；当前 epoch 不热改配置。具体坡面、盒尺寸和接触测试在切片中固定。
- RGB 按已应用的权威状态异步采集，携带运行代次、步号、模型时间、载具/传感器/帧身份、实际采集位姿、尺寸/编码、内参和安装外参。墙钟采集/传输时间单独记录。
- 丢帧可见，旧 epoch 拒绝，重连只交付当前帧；显示或图像断流不决定物理节拍。
- 未来必需动态环境反馈缺失或过期时冻结所属联合场景，并要求显式恢复。仅显示/图像断流不因该反馈合同冻结物理；真实主机资源不足仍遵守此前 #8 的监督预算。

这项答复不替代 #6/G6 正式数值预算，也没有批准未知 DLL ABI、未核实样本对应关系或硬件条件。RGB 分支的环境决策已取得；#9 的 DLL/插件部分仍开放，#29 仍保留 #23 数值前置。Full 与所有 HIL/SIH 义务保持原范围，不以这份批准记录宣称已实现或验收。
