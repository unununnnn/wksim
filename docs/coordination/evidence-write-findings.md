# 第七场同步日志写入观察

`aruco-track-c7466446af` / `30b9ba60b780465a908a1274db51b6e5` 在tick4860、累计迟到101.200156ms失败，未解锁/未启用RGB，已退场。此次只启用实际write调用计时，没有更改缓冲、载荷或倍率合同。

监督器实际记录：rate 2436次write，clock 4861次，lifecycle 138次，public-dds 2399次，wire 18266次。超过1ms的调用仅两次：

| 流 | tick / write序号 | 本次字符 | 实测耗时 | 调用后底层偏移 |
| --- | --- | --- | --- | --- |
| rate | 4764 / 2386 | 401 | 10.180203ms | 906753 |
| wire | 4848 / 18219 | 491 | 3.168110ms | 7683876 |

用相同WSL Python版本、UTF-8 TextIOWrapper与65536字节BufferedWriter，将原始逐行文本重放到内存RawIOBase。两次慢调用的累计字符数和底层偏移都与实测精确一致；分别触发65100与63650字节的底层write。原始重放见`validation/coordination/write-boundaries-07.json`，工具为`tools/replay_write_boundaries.py`。该实验没有磁盘写入数据流，证明缓冲写出位置，不把耗时直接归因于磁盘硬件。

这些同步调用占用了受倍率监督的时间，但约13.35ms不能单独解释全场101.2ms累计迟到。下一实验将仅对JSONL证据引入有界、无丢失、顺序不变的后台写入；队列满或I/O失败必须显式使运行失败，完整关闭并退场后才能接受日志。状态、操作请求、控制消息、物理步长及100ms规则不进入该改变。其它耗时原因仍待验证。
