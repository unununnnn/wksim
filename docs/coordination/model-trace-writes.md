# 模型轨迹写入不再阻塞逐步回复的实验

第12场AP重复运行已进入相机跟踪并取得32帧，但在tick62348、累计迟到100.015309ms触发原保护，仍为失败。已清理并保留原件，不继续无变化重复。

`worker.py`原实现每步先向行缓冲文件同步write，再回复该步JSON RPC。新显式`--async-model-evidence`实验复用已验证的有界证据写入模块，保持原输入、步进、回复与轨迹内容。默认模式仍同步。每次新请求前检查后台错误；正常EOF时必须完整关闭，2s关闭上限保持在原模型退出等待范围内。模型主线程仍FIFO/40，启用reset-on-fork防止后台记录线程继承实时等级；飞控策略不改。

两个独立真实模型进程依次执行相同1,000步固定输入：一组同步、一组后台写入。全部RPC与包含实际输入的轨迹逐字节相同，分别核对SHA；后台提交/写出1,861,612字节、队列高水位2、线程正常退场。原件及源码身份在`validation/model-async-trace-equivalence-01`。相关43项WSL检查通过（2项条件跳过）。这证明本次写入方式变更没有改变受控重算结果，不是G6物理精度或飞行通过。

运行时与独立物理审计均要求两个模型writer摘要与文件一致、完整落盘且线程退场。下一真实AP场景使用原资源与门槛，只显式启用这项写入实验，结果另行验收。
