2026-09-05，主代理验收通过。

保留原47个Prometheus schema，新建3个wksim包络类型，公开请求、事件和状态带run/epoch/request身份。新候选/root/wksim-ros2-2egljG真实构建与双栈飞行通过；legacy过渡明确，不静默补身份。正式配置restart_control_on_ground显式在解锁前重启控制进程，FC/physics进程及模型时间持续，新请求才接管。

双栈实际进程重启、旧epoch ARM/MOVE、其他run、旧接口、重复包络/MOVE ID均有负例。被拒目标不产生新设定值，已接受的合法悬停继续保活。PX4原生命令identity跨进程为200/1到200/7不复用，实际旧ACK拒绝；新pending/同命令晚ACK另有边界检查。AP迟到响应有真实本地RMW延迟服务fixture，不冒充对FC回复延迟注入。源/接收时钟分开、重复/回退不刷新旧缓存，本地撤销不声称召回FC内已接受动作。

AP证据validation/product-epoch-nuimpt4w/acceptance.json SHA256 21c85803adbd806023e169cc61d57a88bfeb8a9754445b4eb12205c573012397；PX4证据validation/product-epoch-wzuq0gkv/acceptance.json SHA256 e614ebf23a186111dab5c5c14d501754d1f84b28dbaab184d705b01cc39d9ed5。当前源码审计通过，130项回归无跳过，双栈UE回归通过。

限定为解锁前地面显式重启，无空中恢复/自动抢控承诺。AP旧进程在主动TERM时记录Humble关停context异常并退出1，已经回收和释放锁；保留日志缺陷，不说所有子进程优雅exit0。命令、失败记录、清理、接口契约见本机docs/wksim-control-session.md及docs/2026-09-05_product-second-wave-report.md。未提交/推送混合工作区。原Wayfinder#1、规格#10和Full Goal不关闭；下一前沿#15多航点/取消。
