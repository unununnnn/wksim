# 首期移植批次与验收矩阵提案（#6 决策输入）

2026-09-07。本文档把已完成真实证据汇总为 #6「双飞控 SITL 首期验收与迁移顺序决策」的具体提案，供用户一次性确认取舍。数据来源为各轮已发布报告与 GitHub 票据状态；标注「已验证」的条目均有真实双栈运行+原始审计，标注「待实施」的条目不据既有证据宣称通过。

## 一、首期最小机型与场景（提案）

- **机型**：四旋翼 X（唯一已端到端验证构型；六旋翼/其他构型在 Full 批次另行验收）。
- **场景**：`joint_quad_dds_v1` 联合场景（PX4+ArduCopter 同场景权威时间）与 `independent_quad_dds_v1` 单机场景；UE5.5 P450 机体/旋翼真实资产显示。
- **任务入口**：同一正式实验配置分别选择两套真实飞控（公开 Prometheus 任务，观察者不代发）。

## 二、#6 点名项的当前证据状态

| #6 点名项 | 状态 | 证据 |
| --- | --- | --- |
| PX4 真实 SITL 起飞/悬停/航点/降落 | 已验证 | #12 及后续各轮；最新共同时间 `bqozl_ek` 82216tick/27.002s；独立 `independent-flight-px4-20260907-run1` |
| ArduCopter 同上 | 已验证 | 同上；AP 独立实飞 run1 |
| Prometheus 控制/消息/任务代码保留 | 已验证 | 固定基线 5dcd8cfa…；真实控制包安装候选 79 项矩阵；任务新请求身份逐条原生 ACK |
| UE5.5 模型/场景显示 | 已验证 | 单机 #17、联合三证据（run8/9/10，回读↔真值逐机一致） |
| 地面站（只读遥测） | 已验证（协议级） | #42 原生遥测出口 8 次/40 组审计；**QGC 界面查看待受控构建 HITL** |
| 记录回放/证据回看 | 已验证 | #16 离线回看关闭 |
| 超时与断连处理 | 已验证 | #22 关闭（修订后双侧 0.5× 监督通过+审计）；模型/FC 死亡 input-stall 系列 |
| 禁用 mock/版本可追溯 | 已验证 | 每轮 source_sha256/二进制哈希/原始审计；失败样本全保留 |
| MATLAB 仅契约验收 | 已验证 | #41 桥关闭（首期白名单范围，真实 R2022b 联测） |

## 三、首期控制/任务能力批次（提案顺序与现状）

以下全部只需任务级真实运行（基础设施已就绪），按依赖排序；阈值沿用已冻结数值（100ms/±2%/±1% 倍率、驻留/接管门槛、0.5m/s 恢复驻留），**不新增或放宽**：

| 批次 | 票据 | 前置 | 实施内容 |
| --- | --- | --- | --- |
| C1 | #32 速度/偏航、#33 混合轴/轨迹 | 均已满足（#12/#14 closed） | 同一 Prometheus 命令双栈速度阶跃/回中/偏航；混合轴未激活轴不为零目标 |
| C2 | #34 姿态/推力、#38 RC 交接、#47 全球航点 | C1 + #12/#14 | 姿态/推力 profile 双栈出口；RC 显式交接不抢模式；经纬度+home 相对高度 |
| C3 | #35 PID、#36 UDE、#37 NE | C2(#34) | Prometheus 三控制器经姿态/推力出口闭环对比 |
| F1 | #44 电机效率、#45 GNSS | #22(closed)、#24 | 固定事件计划真实故障注入与恢复观察 |
| M1 | #23 数值基线→#24 参数→#25 六旋翼→#26 生成模型→#27/#28 DLL | **G6 数值预算仍待批准**（ZIP11.0/SLX11.8 来源不成立） | 模型链批次；DLL 依 #9 插件部分决策 |
| V1 | #31 深度点云 | #30(closed)、约定待批准 | 见 #31 约定提案（另发） |

## 四、阈值与预算现状

- **已冻结（不再更动）**：联合倍率监督 100ms/10s±2%/60s±1%（含 start-recovery-task 重锚修订）；任务驻留/接管/恢复门槛；恢复驻留 0.5m/s。
- **仍待用户**：G6 数值/动力学等价预算（#23 的物理误差预算；现有 ZIP/SLX 来源不足以充当独立数值参考）；1× 持续倍率走向（47.1s/60s，宿主层）；#21 验收 3 措辞；#42 受控构建授权。

## 五、请用户确认的取舍

1. 是否批准第一节的首期最小机型/场景与第二节状态表作为**首期验收矩阵基线**（已验证项不再重复飞行，仅作回归）？
2. 是否批准第三节的批次顺序（C1→C2→C3 控制链先行，F1 故障并行可插空，M1 等 G6 预算，V1 等约定批准）？
3. 对第四节四个待批项的逐个答复（可分开回答，不互相阻塞）。

批准本提案不关闭 #20/#21/#42 及 G2–G6 任何门槛；未批准项维持真实阻塞，不缩减 Full 终态。
