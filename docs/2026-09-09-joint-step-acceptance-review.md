# #20 原票验收复核（2026-09-09）

结论：**#20 暂不具备关闭条件；明确剩余项为批准的持续 1× 倍率验收。** 暂停、四 tick 单步、继续、批准的暂停监督、冷重置及显式恢复不重放已有真实证据，不需要重新发明验收门。2026-09-08 用户已选择如实记录宿主限制、1× 保持开放先推进其他切片；这不是取消 1× 要求。

本次为有界只读复核，只新增本文；没有启动仿真、重跑原始审计、修改源码或 GitHub。主代理已核验本代理实际 gpt-6-astra/low 后释放工作。读取了 AGENTS.md、CONTEXT.md、计划最新状态、`gh issue view 20 --repo unununnnn/wksim --json body` 及其评论和下述精确路径。未进行未知代码结构探索，因此没有调用结构索引。复核时 HEAD 为 `6fa0ce01b534b5fbcbdbcbe8ae8094c1755cff13`；引用的报告/合同已跟踪且无 HEAD 差异。`validation/` 原始证据为本地保留文件，所查摘要不在 `git ls-files` 中，不能称原始数据已提交至 Git。

## 原票逐项判断

| 原 Acceptance criteria | 判断与直接证据 |
| --- | --- |
| 暂停物理不推进、单步只推进批准权威时间量、显示不改变步数 | 已有实证。正式入口 `product-joint-flow-vd3464wq` 两次 4s 冻结，51856→51860 严格四个 1ms tick，继续完成双栈航点与落地，69528 tick。后续倍率生命周期 `joint-rate-flow-82p4pbu7` 同样四步且原始审计通过；`ox8h58cv` 为后续 0.5× 生命周期通过样本。显示独立性另有已关闭 #21 的真实 UE 证据，不另设新的 UI 操作门。 |
| 暂停保活、墙钟看门狗、FC 遵循批准策略并有真实记录 | 已有实证。健康许可按 100ms 发布/500ms 有效；两次暂停保留原任务并在新原生确认后继续。许可撤销样本 `joint-public-flight-8a572x9j` 撤销控制、不自动恢复；真实 Agent 失联与新接管另有双侧原始审计。见批准生命周期报告及正式入口报告。 |
| 冷重置新代次重新就绪、不恢复旧任务 | 已有实证。`product-joint-flow-61t52y3j` / `32s6alit` 完整退场后创建新 net/ipc/mnt 与 Control epoch，重新地面就绪，旧操作拒绝，无 Task/解锁/旧任务 ACK；旧 namespace 句柄保留避免编号复用误判。`joint-rate-flow-td2w1nim` 另验证旧 epoch 的 1× 请求被拒绝，新 epoch 保持 0.5×。不要求热复位或空中热恢复。 |
| 倍速、恢复无历史动作补发且时间偏差在批准门槛 | **部分满足，1× 未满足。** 0.5× 三独立 epoch 的真实连续空中 60s 窗、全部 10s 窗已通过（历史串行 RPC 源快照）；变速/暂停/继续、过载冻结、冷重置及批准修订后的双侧恢复均有实证。1× 三场仅 47.1/41.1/23.7s，三场审计均 failed，不能把正确冻结当倍率成功。 |
| 命令、版本/身份、预期结果、失败/未验证边界，主代理复核后关闭 | 交付材料已存在，本报告补齐原票映射。最终关闭仍须剩余倍率成功证据与主代理复核；本报告不替代该步骤。 |

## 合同与证据定位

[正式倍率批准](2026-09-07_joint-rate-contract-accepted.md)保留 1ms 物理步、4ms 屏障，0.5×/1× 每档至少三独立 epoch，每次 60 连续墙钟秒有效段，10s 窗 ±2%、60s 段 ±1%、累计相位迟到不超过 100ms；所有失败窗须报告，不拼接暂停两侧、不以地面冒充飞行、不用短时 1× 切换冒充持续验收。原获批副本 SHA256 为 `54dcd1df7d70caeb483ab101e7071f8d48168f29e22a93da594e455a459a02a5`。后续 start-recovery-task 修订仅增加显式重锚点，数值不变。

以下 SHA256 为本次从现存文件实际计算；历史审计的通过状态来自已留存结果，本次没有重跑：

| 文件（仓库根目录相对路径） | SHA256 |
| --- | --- |
| `validation/product-joint-entry-20260906/final-audits.json` | `d3ecd88854b8a3393a8974820eeb38359fd5af0a0617042e9087f33840cc1670` |
| `validation/migration-resume-20260907/rate-half-cohort.json` | `6a33cc14945285ab9c4a76a8ba101d3ede32e194f45764c8fd605138a6436ae4` |
| `validation/rate-1x-retest-20260907/audit.json` | `283185fe0f64c643f6633ce917bc705e9a68767db28f5f7e8795aa1080eed8d5` |
| `validation/recovery-audit-20260907b-_yrr1_v8-a.json` | `0f84c8e20317878d3ffe2daff2ef0fd613e47dbc956bef674c58cd8a2302949f` |
| `validation/recovery-audit-20260907b-ffi_57_3-a.json` | `4765a72ffb704567318d5a7376dd2ed61d89f093325628265864ce2fc01494d2` |

`final-audits.json` 给出三场各两次实际审计 argv 和运行源码/映射身份，健康/AP/PX4 的重复审计摘要分别 `195b6767f9e4539c70b1f4e9377a097063321940f8da3daff33fb9d4a2431a56`、`d3a14b963bca822f04905a0e78b959749fdcf797acd9790532d06abd4d62a978`、`4a4245196d3cca31e9f4c15ca9d0116d064887ab28469b443cd8d13390f69147`。实际候选清单和完整命令见[正式入口报告](2026-09-07_joint-product-entry-report.md)。0.5× 三场最坏迟到 28.607132/53.680553/66.825204ms，仅代表对应历史源快照，不能自动证明后续组合候选。

恢复状态应采用[归因纠正报告](2026-09-07-recovery-staging-revert-correction-report.md)：AP `_yrr1_v8` 与 PX4 `ffi_57_3` 在修订后通过，锚点段最坏迟到 94.87/61.60ms。先前三次 recover 窗口失败源于已回滚 staging 实验；保留失败，但不再把撤回的“恢复窗口合同问题”列成阻塞。AP 后续 100.13ms 漂移失败、PX4 两次 0.526/0.508m/s 驻留失败亦不能抹去。

## 最短剩余工作

按[已批准推进策略](2026-09-08-five-decisions-accepted.md)，当前可先完成其他票，#20 保持开放。重返本票时只针对持续 1× 的真实瓶颈做工程改进/资源配置，在冻结源码、候选身份、负载、驻留配置和原预算后取得三独立 epoch 的完整合格窗口，保留所有失败，并用原始审计验算全部窗口及不补发行为。若更改调度/重锚/核心接口，再对受影响的 0.5× 与生命周期做针对回归；不盲目重做已成立的全部 UI、冷重置、传感器或 G6 工作。

本复核不把 2×、任意网络故障、环境碰撞、硬件或 G6 当作 #20 新增关闭门；它们仍按各自已批准 Full/其他票据义务处理。亦不把“资源不足正确失败”解释成取消已批准持续 1× 验收。
