# 计时器末段等待检查

2026-09-09。为排查最终组合P+V的累计迟到，比较了原 `JointRate.begin_group` 与减少等待环重复 `check(max(...))` 的候选实现；100ms门槛、8ms最小起始间距、分组/tick和健康回调约定未改变。

无飞控/模型/ROS的1000组基准中，两者均保持最小起始间距8,000,000ns。原实现最坏迟到1,161,983ns，候选1,928,489ns；CPU时间分别约1.0381/1.0385秒。没有可证明收益，因此已恢复原计时器原字节，未将优化投入后续运行。原/候选事件和基准脚本保留于 `validation/rate-release-efficiency-20260909/`。

12项确定性计时边界检查通过，新增的oversleep检查保留：等待中超过100ms必须在创建下一组/执行物理前失败；精确100ms仍按原合同处理。初次命令误带不存在的 `test_joint_rate_audit` 模块，随后以真实存在的测试模块完整重跑通过，未将加载错误算作通过。

此结果不是飞行或性能通过证明。最终组合三次实飞失败仍保留原结论，不更改已批准倍率合同。
