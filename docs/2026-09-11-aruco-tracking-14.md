# 第14场：发布者身份通过，倍率失败

AP选中试验 `aruco-track-dfb7b2fcc3` / `71d893d65ce2497295bf38bd903e49f7` 在tick65452触发 `rate_unmet/resource_insufficient`，迟到100.165787ms，55帧。驱动退出1，管理器正常退出0、无清理错误；原失败不改判。

新增writer绑定实际生效：AP177次/PX4135次快照全部通过独立检查，完整固定频道、真实类型、具体Control节点/完整GID、各次图中唯一发布者与全部对应raw样本一致。结论只限所记录的图采样时刻。两模型writer均完整退场、队列高水位2；AP一次后台底层写耗时23.225556ms，但不能据此断言监督器或模型主线程被该写入阻塞。

用真实JointRate重放精确复现本场失败。对比第13场，本场组执行中位数3.408519ms（13场3.375541ms），超过8ms的组14个（13场9个），前组超时总量49.799251ms（13场17.619477ms）。最大的相位增量出现在解锁附近tick47316/47188/47192，分别约12.16/9.93/7.92ms；tick47312的组执行20.037487ms。此时尚未启用RGB，不能归因为相机处理或最终HOLD。没有启用分阶段CPU探针，当前不能进一步指认模型/监督器/原生等待或操作系统调度。1s图快照及每次发送前查询也未经因果隔离，不把它们排除或定为根因。

下一场沿同源码和资源开启既有 `--cpu-timing` 观测以分解上述执行峰值；不改物理步长、屏障、100ms累计门槛或追赶/重锚规则。

原件 `validation/40-aruco-tracking-14-ap` 共442文件、6个归档分片，SHA256 `52c8d1ad81e433e03d7679815890cb9004fdb636cdbd497d287bcc5c925bc543`。诊断为 `validation/coordination/aruco-14-ap-{publishers,rate-replay,arm-timing}.json`；13场重放对照为 `aruco-13-ap-rate-replay.json`。本场未到终态HOLD，不能验证该新等待最终通过，也不满足#104完成条件。
