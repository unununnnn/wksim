# 首期联合场景墙钟监督值批准记录

2026-09-06，用户对会话中的第二项明确问题回答“批准这些值（推荐）”。本记录只确认该问题列明的数值及超时冻结/故障、不自动重放原则，不推导第一项任务暂停/恢复语义也获批准。

| 项目 | 已批准首期值 |
|---|---:|
| 场景生命周期许可发布间隔 | 100ms |
| 场景生命周期许可有效期 | 500ms |
| 原生状态新鲜度 | 保留现有2s |
| 单个模型RPC等待 | 3s |
| 飞控输入屏障等待 | 5s |
| 显式恢复就绪等待 | 5s |

用户同时接受问题中列明的边界：超时冻结或故障，不自动重放；这些是待验证的运行参数，不是动力学误差预算。它不证明所有参数已集成或运行通过，也不允许测试失败后事后放宽。

问题来源：[暂停与恢复提案](2026-09-06_joint-pause-contract-proposal.md) P4。先前1ms权威tick/4ms输入屏障、新epoch冷重置等原则仍依照[既有批准](2026-09-06_recommended-decisions-accepted.md)。

第一项问题“有明确场景许可的用户暂停保留当前目标与剩余驻留时间；真实异常冻结撤销控制且恢复需新请求”等任务语义，尚未收到该项答复。不得将本次数值批准扩展为该项整体批准。#8及G2/Full仍需按完整接缝实现和验收。

## 随后的独立语义批准

用户随后对第一项问题明确回答“批准此语义（推荐）”。因此下列语义也已批准：有明确场景许可的用户暂停保留当前目标与剩余驻留时间，显式继续后再用新原生样本确认；迟到、掉队或真实失联则冻结全场景并撤销任务控制，恢复物理后必须另发新任务请求；模型部分完成或时钟倒退只允许停止/冷重置。

本段以该项独立用户答复为依据，补齐前段当时未答复的状态，不从数值批准推导授权。两项批准允许进入该接缝的实现和真实联测；不代表实现已完成，也不关闭#8/#19/#20/G2/Full。
