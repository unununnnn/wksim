# 第八场：真实跟踪内容成立，收尾倍率仍失败

本场 `aruco-track-32addd9c38` / `901d6d10a3934976a899633020d97657` 已完成双机起飞、相机跟踪、遮挡/恢复与降落；两个Task报告均为pass。但主监督器在tick78248记录累计迟到103.945ms并触发原100ms保护，整体保持failed，不关闭#104/#40。

保留85帧RGB，其中70帧有目标；独立内容检查的四段样本为12/28/15/30。检查原始PNG、场景读回、通知身份、像素/PnP重算及真实逐tick轨迹：PX4最大跟踪误差0.426228334m，最终恢复窗最大误差0.046668672m，均满足运行前冻结的0.65/0.3m门槛。两机12,001个跟踪区间物理样本均通过范围/高度/倾角检查且最终落地。结果只标`diagnostic_only`，原始rate故障完整保留，见`validation/coordination/aruco-08-content-inspection.json`。

五个后台证据写入器均正常退场，submitted/written字节数与实际文件一致。监督器write调用最大值均小于1ms，writer后台单次I/O最大约1.81ms。它消除了第七场所观测的同步慢写形状，但不能据此宣称整个倍率问题已经解决。

另发现受限时主循环在worker退出和完成检查中直接解析完整报告。PX4报告6,963,444字节、14,194条adapter动作；同机离线读取与解析实测22.58–30.55ms，见`validation/coordination/aruco-08-report-read-cost.json`。这不是本次23.47ms末组停顿的唯一根因证明，但该负担不应位于100ms受限时循环。

后续实现将ArUco的运行中完成判断限定为真实worker已以0退出；可信入口只会在报告写完后返回0。完整报告在停止并清理后读取，仍核对run/epoch/stack/退出状态，错误仍使整场失败。不会删减动作记录、放宽门槛、自动重锚或跳过独立审计。其它任务保留现有读取行为。
