# #23 关闭后的可执行前沿

2026-09-09 JST。通过 `gh issue list --repo unununnnn/wksim --state open --limit 100 --json number,title,body` 读取 #11–#48 的全部 19 张开放实施票，并逐票读取 `gh api repos/unununnnn/wksim/issues/<number>/dependencies/blocked_by`。以下依赖状态来自原生关系返回，不从标签或旧正文推断。读取当前 `docs/plan/README.md`；只新增本文，没有运行模型、测试、构建、ROS、索引或修改工单。

**优先推进 #24 的模型配置包，其次 #40 的感知输入包、#45 的传感器事件包。三个包可以先实现并离线验证，不需要重复联合 0.5× 飞行，也不占用 #34 的原生固件构建。它们是可交付实施增量，不是三张票已具备立即关闭条件。** #23 关闭的是严格数值对照交付；R1 仍 numerical_failed，G6/物理精度没有通过。不能把这点变成新全局等待，也不能把后续参数响应、感知或故障测试冒充物理精度通过。

## 排名前三的具体包

| 排名与票据 | 原生依赖实读 | 现在实施及可检验终点 | 已读接缝与仍需解决的具体问题 |
| --- | --- | --- | --- |
| 1 · [#24 四旋翼参数保存导入与真实运行](https://github.com/unununnnn/wksim/issues/24) | #12、#23 均 CLOSED；无开放依赖 | 实现具名四旋翼参数配置、严格单位/范围验证、保存/导入及模型来源摘要；先完成往返相等、非法/缺字段拒绝、不同参数不能沿用同一模型身份的离线验证。然后单独安排一份私有模型候选的静态响应比较，不用双飞控联合倍率作为该包前置。 | `Simulator/wksim_core/model.py` 和 `model.cpp` 当前只有 create/step/destroy，create 无配置参数，不能仅保存 JSON 就声称生效。`docs/plan/model-reference-provenance.md` 及 `validation/model-reference-provenance-20260907/parameter-correspondence.json` 有 22 项映射；下一实现需实读固定生成头中的参数可写性/初始化顺序，处理共享静态参数的一进程一载具边界。不修改默认模型/其身份 pin。静态/飞行观测须在运行前声明；完整票据仍需要真实受控响应。 |
| 2 · [#40 真实相机驱动ArUco目标跟踪](https://github.com/unununnnn/wksim/issues/40) | #30、#32、#6 均 CLOSED；无开放依赖 | 冻结一个标记 ID/尺寸/坐标和标定输入，建立图像到机体系目标的最小独立消费模块；用固定标记图验证投影/外参、未知标记拒绝、过期/遮挡时撤销目标、恢复不复用旧目标。输出保留图像 run/epoch/step/stream 身份；后续接既有公开速度任务入口。 | 已读 `Simulator/ue55/rgb.py`，现有 Reader 已检查通知与元数据身份、代次和帧步递增；复用 `docs/rgb-geometry-fixture.md` 的几何边界。现有 RGB 测试场景不等于已有 ArUco 场景。Prometheus 检测/跟踪原源、检测依赖版本及标记材质尚需下一实现者定点定位/实读，不预称已有可迁移模块。这一离线包不用启动 UE、固件或 ROS；完整 #40 仍须真实相机至飞行闭环。 |
| 3 · [#45 GNSS中断与状态有效性恢复](https://github.com/unununnnn/wksim/issues/45) | #22、#23 均 CLOSED；无开放依赖 | 先实现独立 GNSS 事件计划与输出决策：固定权威 tick 开始/结束，保留原始源时间与质量，区分停止新 GPS、无效定位和过期数据；离线验证边界 tick、重复输入、恢复及新代次清除。为 PX4 的实际 HIL_GPS 发包边界准备接入，记录原包与实际发包决策。 | 已读 `Simulator/wksim_core/px4_mavlink.py`：`gps_arguments` 使用模型 GPS 输出，发送周期为 100 tick；`Simulator/wksim_core/ap_json.py` 只向 AP 发 position/velocity/IMU/quaternion，没有独立 GPS 字段。AP 的 GNSS 注入必须先核对其真实原生传感器生成路径，不能删除 position 或伪改显示标志冒充 GPS 故障。因此 AP 适配是明确剩余工程问题，不承诺无固件修改就能完整关闭。先新增纯事件模块及针对性验证，实际接入由主代理串行分配核心文件写入。 |

以上路径均相对本仓库根。排名考虑独立性和未知量：#24 直接解锁 #25/#26/#44 的模型工具前置；#40 不触碰当前原生控制编译；#45 的 PX4 出口清晰，但 AP 注入尚需源码证据，所以排第三。三个包不需要额外例行批准；共享源写入与运行资源继续由主代理分配。

## 全部开放实施票的真实依赖摘要

| 票据 | 尚 OPEN 的直接依赖 | 已 CLOSED 的直接依赖 |
| --- | --- | --- |
| #20 | 无 | #19、#8 |
| #24 | 无 | #12、#23 |
| #25 | #24 | #17 |
| #26 | #24、#9 | 无 |
| #27 | #9 | #12、#23 |
| #28 | #27、#9 | 无 |
| #29 | #9 | #17、#23 |
| #33 | 无 | #32、#6 |
| #34 | 无 | #12、#14、#23 |
| #35 | #34 | 无 |
| #36、#37 | #35 | 无 |
| #38 | 无 | #12、#14、#6 |
| #39 | #29、#33 | #15、#6 |
| #40 | 无 | #30、#32、#6 |
| #44 | #24 | #22 |
| #45 | 无 | #22、#23 |
| #46 | #20、#24 | #16 |
| #47 | 无 | #12、#14、#23 |

不将 #33 最终组合三轮倍率失败再安排成盲重试；#34 正在独立候选构建，留在现有写入线。#20 仍有实际倍率/生命周期验收义务，不能因依赖全关闭就视为低资源收口。#38 尚须明确 RC 模拟源/通道/失联合同；#47 需要 home 身份及双栈原生适配，会与当前控制代码线重叠，适合 #34 接缝稳定后接续。#9 仍 OPEN，故 #27/#29 等不被 #23 关闭自动解除全部依赖；#35–#37 也不能绕过尚 OPEN 的姿态出口链。

本报告没有宣称上述候选模块已实现，也没有执行这些建议测试。源码仅按已知文件路径读取；报告不是结构图查询，未为此触发索引或重建。
