# wksim 仿真工具链

本上下文描述以 Prometheus 为移植主体的仿真工具链及其兼容性边界。

## Language

**完整功能复刻**：覆盖已冻结的公开完整版功能和用户操作流程，定制版专属能力另列扩展；不要求复制品牌、原界面布局或跨平台浮点结果逐位一致。
_Avoid_: 原源码恢复、二进制逐位复制

**自主仿真核心**：能够独立构建运行、承担载具动力学和传感器仿真的核心，不以原版CopterSim程序或闭源模型为必需条件。
_Avoid_: 原版程序启动包装器

**模型插件**：可导入核心的载具模型，原版DLL属于可选插件，不是自主核心成立的前提。

**对照程序**：用于观察参考行为并验证兼容性的原版程序，运行对照成功不代表自主实现已经完成。

**模型源码**：描述载具、环境、传感器或控制模型的可编辑源材料，与承载模型的完整仿真应用源码不同。

**数值对照验收**：在固定模型、输入、初态、随机种子和时间条件下，按预先约定的误差阈值比较动力学与传感器结果；不同于“能运行”或“报文能解析”。

**联合场景**：多个载具处于同一个环境、可能相互影响并共享一条权威仿真时间线的运行。
_Avoid_: 并行独立实验

**独立实验**：具有独立环境和时间线、结果互不影响的一次仿真；多个独立实验同时运行不构成联合场景。

**权威仿真时间**：联合场景中物理状态、任务和传感器共同参照的时间线，不等于显示帧时间或计算机墙钟。
_Avoid_: 渲染时钟、各载具本地计时

**命令受理**：任务控制层通过校验并保存命令，不表示飞控已接受，也不表示动作完成。
_Avoid_: 飞控确认、任务成功

**动作完成**：载具的实际反馈在规定时间窗口内持续满足任务目标及误差门槛；不同于目标报文已发布、飞控受理或瞬间经过目标。
_Avoid_: 命令受理、目标已发送

**定位就绪**：载具当前估计位置、速度和姿态满足指定使用条件的状态；不等于某个飞行模式允许进入，也不等于允许任务控制接管。
_Avoid_: 全部飞行条件就绪

**任务接管**：任务控制层在飞控模式、状态和姿态对齐满足条件后开始持续提供控制目标；不同于飞控独立执行起飞，或任务命令仅被本地受理。
_Avoid_: 已解锁、命令受理

**期望参考量**：由 Prometheus 命令解析出的有效位置、速度、加速度、姿态或降落意图；还需输出修整和飞控适配才能成为原生控制输入。
_Avoid_: 已发送的飞控设定值

**输出修整**：将期望参考量转换成具有明确激活轴的控制目标，包括速度死区、位置保持与偏航保持；不代表目标已通过通讯到达飞控。
_Avoid_: 飞控确认、坐标系转换
